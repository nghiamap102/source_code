import { mainColor } from 'src/theme/theme'

export const Input = {
  baseStyle: {
    field: {
      ':hover': {
        borderColor: mainColor.gray3
      }
    }
  },
  defaultProps: {
    variant: null // null here
  }
}

export const PinInputField = {
  baseStyle: {
    field: {
      ':hover': {
        borderColor: mainColor.gray3
      }
    }
  },
  defaultProps: {
    variant: null // null here
  }
}
